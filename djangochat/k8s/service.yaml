apiVersion: v1
kind: Service

metadata:
  name: django-chatops-service
  namespace: chatops
spec:
  selector:
    app: django-chatops

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

  type: NodePort
